---
date: 2026-08-24
tags: [journal, implementation, release]
---

# 2026-08-24

## Phase 7 Rollout Foundation — Isolated Commit

After context-contract and architecture review, Phase 7 was moved out of the mixed shared worktree into clean worktree branch `codex/phase7-release-foundation`, based on local `origin/main`.

Committed local-only as `fcb49a9` — `feat(operations): add fail-closed rollout foundation`.

The commit contains:

- `FeatureRollout` and branch override models with updater foreign keys.
- Fail-closed feature environment evaluation, server-derived branch scope, SuperAdmin-only mutations/cross-branch selection, strict payload validation, secret-like reason rejection, CAS, audit redaction, and paused/disabled rollback precedence.
- Prisma migration plus CLI-generated canonical Supabase migration. The Supabase migration enables RLS and revokes `anon` and `authenticated` privileges; it was not applied.
- Route and middleware coverage for SuperAdmin success/denial, create/update, branch override update/CAS, stale writes, role-minimum/effective state, and secret rejection.

Verification on the exact clean commit candidate:

- Prisma validation and client generation passed.
- TypeScript/API build passed.
- 139 API tests passed.
- `git diff --check` passed before commit.
- Independent static quality gate passed.

## Still Not Release Evidence

No migration was applied, no database/provider was touched, no push/PR occurred, and no deployment occurred. Required next gates are a disposable clean PostgreSQL/Supabase migration test, RLS denial checks, backup/restore proof, browser/API role smoke, and approved pilot release. The shared worktree remains untouched and mixed; Phase 1–6 still require their own clean isolation.

[[Home]] | [[Context]] | [[Planning/Release-Phase-1-7-Isolation-Manifest]] | [[Planning/Phase-7-Cross-Phase-Release-and-Operations-Spec]]

## Phase 1 Computer Manual Intake — Isolated Commit

Phase 1 was rebuilt in clean worktree `codex/phase1-computer-intake`, based on `fcb49a9`, and committed local-only as `1f6b6a6` — `feat(inventory): add gated computer manual intake`.

The commit adds default-off rollout gating, company-only desktop/laptop intake, server-scoped lookups and draft/asset access, duplicate-name warnings, normalized asset-tag protection, CAS plus idempotent transactional submit, typed profiles/components on asset detail, and redacted activity evidence. Its Prisma migration has a read-only duplicate-tag preflight; the canonical Supabase migration was scaffolded with `supabase migration new` using `--workdir`, then populated with equivalent tables, constraints, indexes, RLS, and revoked public/client grants.

Verification:

- Prisma validation and API production build passed.
- 148 API tests passed, including 9 focused Phase 1 tests.
- `git diff --check` passed before commit.
- Static quality gate passed after Prisma/Supabase timestamp parity review.

The frontend check was attempted with installed dependencies but remains blocked by the known local Windows/esbuild workspace-access error, not treated as a pass. No migration was applied, no branch was pushed, and no deployment occurred. Remaining release gates are clean database migration/preflight, backup/restore, browser/accessibility smoke, frontend CI verification, and staging IDOR/concurrency validation.

## Owner Decision — Staging Bypass, Production-Only Release Path

The owner approved bypassing the separate Supabase/Vercel staging environment because the live project has no active users or production inventory data. This is recorded as a schedule exception, not a production-readiness pass.

Non-waivable gates remain: confirm the exact Supabase target and its migration history; commit a canonical ordered Supabase migration chain; take a restorable database backup/export; run migration preflight; deploy with expansion flags default-off; verify `/health`, `/ready`, login, and role-scoped routes; retain rollback ownership; and keep Phase 2 Belarc production-disabled pending commercial licensing and evidence-governance decisions.

The next release unit is migration-history reconciliation in a clean worktree. It must precede Phase 7 (`fcb49a9`) and Phase 1 (`1f6b6a6`) and must not pull unrelated Phases 2–6, bulk-import removal, or Department archival changes from the mixed root checkout.

## Production Migration — Owner-Approved Staging Bypass

Verified the connected Supabase account has two active projects: `staging` in Singapore and the older `inventory` target in Tokyo. The Tokyo target was selected deliberately; it had the documented baseline migration versions `20260729131539` and `20260729131623`, and all application tables reported zero rows.

Created and committed the isolated migration-history release unit as `d636a94` (`chore(db): track canonical supabase baseline history`). It tracks the two existing baseline migrations plus the required branch permission migration, without pulling the unrelated import-removal, Department archival, or Phase 2–6 files from the shared worktree. Phase 7 and Phase 1 were rebased onto it as `799689f` and `3de1028`.

Applied, in order, to the Tokyo target through the connected Supabase provider:

1. `20260823175404` — `20260730123000_add_manage_branches_permission`
2. `20260823175436` — `20260823162411_phase7_rollout_foundation`
3. `20260823175443` — `20260823171736_phase1_computer_intake`

The local migration filenames were reconciled to those provider-recorded versions. Verification confirmed the six new Phase 7/1 tables exist, are empty, have RLS enabled, and grant nothing to `anon` or `authenticated`. No rollout record exists and the environment feature flag remains off. The branches remain local-only: GitHub push and any Vercel deployment require separate approval. No production-feature or browser-release claim is made.

The owner then approved the GitHub push. The exact reviewed branches are now remote: `codex/production-migration-history` (`d636a94`), `codex/phase7-release-foundation` (`799689f`), and `codex/phase1-computer-intake` (`3de1028`). They are not merged into `main` and no Vercel deployment was triggered by this push.

## Phase 6 Schema Migration — Owner-Approved Rush Exception

The owner explicitly directed Phase 6 Tools & Stock migration despite the remaining release work. The isolated Phase 6 migration was applied to the same confirmed unused Tokyo Supabase target as provider version `20260823184250_phase6_tools_stock_foundation`.

Post-apply verification recorded zero `StockItem` rows, all seven Phase 6 stock tables with RLS enabled, zero `anon`/`authenticated` table grants, and both new permissions (`view_stock`, `approve_stock_adjustments`) seeded. The feature remains fail-closed: no rollout record was created and `STOCK_TOOLS_ENABLED` was not enabled.

Matching application work was committed as `3b816ea` (`feat(stock): add gated tools and stock foundation`) and pushed as `codex/phase6-tools-stock`. This is still schema/code evidence only: quality review identified missing specified route/page surface and focused regression coverage. Do not enable the feature or claim production readiness from this migration.

## Phase 2–4 Clean Release Branches

The owner directed Codex to take over the remaining direct GitHub/Supabase work. Claude's dirty original worktrees were preserved. Clean release worktrees rebased the isolated commits on merged `main` (which now includes Phase 6): Phase 2 `3e44bd9` PR #16, Phase 3 `0d77219` PR #17, and Phase 4 `ebce1c7` PR #18.

Phase 2 received a canonical Supabase migration with RLS and no public grants but is not applied because commercial Belarc rights and retention governance remain unresolved. Phase 3 received explicit `manage_infrastructure_assets` and `view_sensitive_network_fields` permission seeds for SuperAdmin and Admin. Phase 4 remains default-disabled and awaits Phase 3 merge before network-reference reconciliation and Phase 5 release work.

## Phase 5 Ownership Handoff

The owner reassigned Phase 5 Servers, Firewall, and ISP completion to Claude. [[Planning/Phase-5-Handoff-and-Release-Plan]] records the clean-branch requirement, conservative security contract, required acceptance evidence, and the follow-on Codex release sequence.

The existing local `codex/phase5-release` worktree remains reference-only and must not be released or migrated. Codex resumes after Claude provides a clean commit, pull request, and verification report; provider-side Phase 3, Phase 4, and Phase 5 migration work remains Codex-owned.
