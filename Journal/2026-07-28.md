---
date: 2026-07-28
tags: [journal, deployment, supabase]
---

# 2026-07-28 - Vercel + Supabase foundation

## Verified

- User confirmed Vercel + Supabase as the target stack. Render and Neon stay intact during migration.
- Added a versioned Prisma baseline at oracle-api/prisma/migrations/20260728180000_init_baseline. It is for an empty, verified target only; it has not been applied to Supabase.
- Extracted the Express application into src/app.ts and added a Vercel catch-all entrypoint at api/[...path].ts.
- Added exact-origin CORS, production configuration checks, /ready, deterministic Prisma generation during build, and an explicit one-time admin bootstrap command.
- Removed the fixed seeded super-admin credential. No account or password was generated or stored.
- Validation passed: API build, Prisma validation, TypeScript check including the Vercel entrypoint, and 128 existing API tests.
- Pushed commit fd0609e to codex/vercel-supabase-foundation without modifying the divergent existing feature branch.

## Still blocked before cutover

- Supabase baseline deployment, data restore rehearsal, row/FK/schema reconciliation, and a recovery drill.
- Vercel Preview tests for catch-all routing, auth, multipart uploads, OCR limits/timeouts, imports, and downloads.
- Server-only Vercel secrets and a new, rotated JWT/email configuration. Do not reuse previously exposed credentials.
- Replacing process-local maintenance cron with a verified idempotent Vercel Cron flow.
- Branch/object authorization enforcement and role smoke tests.

## 2026-07-29 - Supabase baseline verification

- Supabase SQL Editor confirmed `to_regclass('public."_prisma_migrations"')` returns `null`; Prisma migration history has not been initialized on the target database.
- This result alone does **not** confirm that the `public` schema is empty. Before applying the existing `20260728180000_init_baseline` migration, record the `public` table inventory and confirm the target is disposable/empty or reconcile any existing schema.
- Safe read-only verification SQL:

```sql
select table_name
from information_schema.tables
where table_schema = 'public'
order by table_name;

select to_regclass('public."_prisma_migrations"') as prisma_migrations;
```

- The initial account path remains: versioned Prisma migration -> idempotent reference seed (roles and permissions) -> one-time `super_admin` bootstrap -> Super Admin creates the first `admin` through the existing user-management surface. No credential values are stored in this vault.

[[Home]] | [[Context]] | [[Planning/Pages/Deployment]]
