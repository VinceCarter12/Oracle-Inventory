---
date: 2026-07-29
tags: [journal, supabase, migration, auth]
---

# 2026-07-29 - Supabase baseline and initial access

## Applied and verified

- Supabase project `inventory` in Tokyo was verified active and empty before the write: no `public` tables and no Supabase migrations.
- Applied Supabase migration `init_oracle_inventory_baseline` (version `20260729131539`), creating the current Oracle Inventory schema.
- All application tables were verified present with RLS enabled. No public RLS policies were added because the current application uses the private Express + Prisma API rather than the Supabase Data API.
- Revoked `PUBLIC`, `anon`, and `authenticated` execution of the pre-existing `public.rls_auto_enable()` SECURITY DEFINER event-trigger helper.
- Applied Supabase migration `seed_inventory_roles_permissions` (version `20260729131623`) for the four reference roles and their permission mappings.
- Created one temporary `super_admin` and one temporary `admin` account, both active and forced to change password on first login. Credentials are intentionally not recorded in this vault.
- Prisma remains the API ORM and schema model. Supabase migration history is now the database migration record; do not run `prisma migrate deploy` against this database unless the migration ownership is deliberately reconciled first.

## Still required

- Verify browser login and forced first-password-change for both roles.
- Push/deploy the local role-management guard before broad user management use.
- Define object/branch authorization, production backup/restore proof, CORS verification, and role allow/deny smoke tests before production cutover.

## Deployment session lessons and migration handoff

- The Vercel/Supabase setup exposed several integration failure modes: unsupported Node 24 builds, runtime database readiness failures, stale immutable deployment URLs, and nested API route handling through Vercel.
- The verified API contract is `/health` and `/ready`; the frontend routes `/api/*` through a Vercel rewrite to the API `/gateway/*` mount. Stable project domains—not immutable deployment URLs—must be used for login verification.
- A reusable client-owned migration procedure, secret-handling rules, test order, and rollback guidance were captured in [[Notes/Client-Account-Migration-Runbook]].
- No credentials, connection strings, JWT secrets, API keys, or bootstrap passwords were recorded in the vault.

[[Home]] | [[Context]] | [[Planning/Pages/Deployment]]

## Sorted quick capture

- The quick capture `I` contained no actionable context; retained here for traceability and not promoted to a task or decision.
